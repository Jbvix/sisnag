O serviço Node único está na **raiz** do repositório (`server.js`, `package.json`).

No **Railway**, crie um serviço a partir deste repositório com **Root Directory** vazio (raiz), comando de arranque `npm start` e ficheiro `railway.json` na raiz (já incluído).

O `package.json` dentro de `backend/` no GitHub histórico era duplicado; use apenas o da raiz.
